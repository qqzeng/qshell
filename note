1. 命令结构，将输入、输出文件描述符
2. 增加一个所有后台程序的执行列表，以方便当执行 exit、jobs、kill 等 builtin命令时，能够基本实现。


//TODO LIST [#//*]
1. 输入命令的时候，有时候不能使用 退格 键
2. 输入命令的时候，有时候不能使用 上、下键
3. 当执行一个后台进程时，如果按下 CTRL+C ，会退出 shell
4. 命令当中不支通配符。比如 ls file*
5. 不支持命令中含有逻辑运行符，比如 & | or and 
6. 暂时不考虑命令当中含有 ; 符号
#. 后台进程


*6. 不合法命令的检测，使得在命令解析的时候，就报错返回，而不是在命令执行的时候。